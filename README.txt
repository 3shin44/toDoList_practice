1.	基本事項CRUD功能
事項可進行新增, 刪除, 修改, 查詢之動作後並顯示於畫面上
2.	標籤與狀態功能
加入狀態欄位(單選下拉選單)
自定義狀態與標籤欄位
3.	內容文字編輯器功能
加入內容(文字編輯器)欄位
4.	呈現樣式切換
將事項列表根據按鈕進行切換顯示方式(呈現方式不限) // light dark mode
5.	事項複製功能
可複製項目(複製規則不限)


-----dev log-----
---20220810---

CodeReview

標籤帶出方式可調整
清除按鈕位置可調整
標籤按鈕工能確認

排版 可以調整一下對齊

代辦事項文字輸入很長的字會破版


dbdesigner.net

資料表 available少一個L

task_revise_history表
有些資料會不見，例如狀態、排序
保留當下的資料即可，給他一個流水號
分年、月、日去存
資料正規化
命名 附表只是流水號功能的話，名字可以不用這麼長，除非有特殊作用(如: 關聯其他資料表)


程式

87 toggle完成狀態切換 命名
eslint prettier

CJK文字編輯器 vue 原生

133 複製事項 傳址

emit xxx_xxx_xxx xxx-xxx-xxx

命名

---20220804---
*使用者自訂標籤
	功能: 使用者可自訂標籤, 之後可針對獨立事件增刪標籤
	基礎架構: 使用者自訂標籤一個獨立版面, 之後獨立事件就可針對已存在的
				標籤進行增刪
	作法: 1. 每個事件要增刪標籤時, 載入現存標籤列表, 並移除重複標籤
		  2. 同1. 但是載入現存標籤列表後, 有被點擊或重複的利用CSS隱藏, 
		  	 優點是不用每次都增刪原始資料

---20220803---
*Ckeditor 功能無法運作

	問題: 原因不詳, 猜測又是跟Vue打架
	測試: 拔掉vue js就正常, vue的問題機率頗高
	處理方案: 用彈窗 避免跟vue在同一個網頁內
	技術問題: 資料怎麼帶過去? localStorage

---20220802---
* Vue Devtool 會消失
在開發者模式用強制清除快取與重新載入

vue-min, common 檔案會造成不同結果, min檔案正常執行, common會出問題
但devTool能穩定跑出來

* Vue-cli專案管理模式的引用不熟練
目前大量套件已經轉為CLI專案模式


---20220801---
*查詢

	方法: 於todos陣列裡, 每個代辦事項的物件中自帶顯示屬性, 最後透過CSS display顯示/隱藏事項
	問題: props沒辦法將屬性推下去, 代辦事項的顯示屬性中顯示undefined

	發現問題: vue在元件模板階段, 無法辨識大小寫, 所以變數名稱只能全小寫,
		  用Snake Case or kebab case

	解決路徑: 其他資料能PROPS下去, 有問題結果的是undefined, 藉此縮小抓蟲範圍

	其他解法: 若觸發搜尋, 將原始陣列篩選後產生新陣列props下去產生結果
		(缺點: 資料結構多一層)



*布景主題切換

	方法: 
		1. CSS 新增對應 變數模板(以黑白主題為例)
		   先選 :root, 並定義各項基本外觀屬性
		   再加入[data-theme="dark"], 並定義另一主題外觀屬性
		2. 在CSS中將要隨主題變動的屬性套用變數
		3. JS設定toggle, 利用前面設定好的主題標記, 切換對應主題內容

		這方法的原理似乎是, 利用root選取到HTML本體, 並自訂一個標籤叫data-theme
		透過JS去切換要使用哪一組變數
		(CODEPEN蠻多這種作法, 有沒有其他方式??? 這要是CSS結構複雜一點就挖結構到死)
		

	備用爛招: JS直接去抓對應class名稱 調整對應CSS屬性